# 变量管理功能修改说明

## 修改内容

### 1. 环境管理下的变量列表
- **不展示变量值**：在环境管理页面的变量列表中，移除了"当前值"列
- **变量记录保留值字段**：后端数据仍然保存变量的值（test、uat等环境的变量记录有参数值）

### 2. 支持同名变量在不同环境
- **环境隔离**：test环境和uat环境可以创建相同名称的变量，每个环境的变量值独立
- **项目隔离**：同一环境下，不同项目也可以有同名变量，值不同
- **唯一性规则**：变量的唯一性由"变量名 + 环境 + 项目"三者共同决定

### 3. 变量管理列表展示
- **继续展示所属环境**：在变量管理列表中，每个变量都显示其所属环境
- **多环境显示**：如果多个环境有同名变量，会以多行的形式展示出来
- **新增所属项目列**：增加了"所属项目"列，方便查看变量归属

### 4. 执行用例时的环境选择
- **设置弹窗选择环境**：在执行用例时，可以在设置弹窗中选择执行环境
- **环境变量读取**：选择环境后，执行时会读取对应环境下的变量值
- **优先级规则**：
  1. 优先使用：环境+项目都匹配的变量
  2. 其次使用：环境匹配+项目为空的变量
  3. 最后使用：全局环境的变量

## 使用示例

### 示例1：创建不同环境的同名变量

**测试环境的api_url变量：**
```
变量名: api_url
所属环境: 测试环境
所属项目: 项目A
变量类型: 固定值
变量值: https://test-api.example.com
```

**UAT环境的api_url变量：**
```
变量名: api_url
所属环境: UAT环境
所属项目: 项目A
变量类型: 固定值
变量值: https://uat-api.example.com
```

**生产环境的api_url变量：**
```
变量名: api_url
所属环境: 生产环境
所属项目: 项目A
变量类型: 固定值
变量值: https://api.example.com
```

### 示例2：在环境管理中创建变量

1. 进入"环境管理"页面
2. 点击选择一个环境（如"测试环境"）
3. 在该环境的变量管理区域点击"新增变量"
4. 填写变量信息（变量值会保存，但在列表中不显示）
5. 保存后，该变量只属于当前选择的环境

### 示例3：执行用例时选择环境

1. 在用例列表页面，点击"设置"按钮
2. 在设置弹窗中，选择"执行环境"（如"UAT环境"）
3. 点击执行用例
4. 执行时会自动读取UAT环境下的变量值（如上例中的`https://uat-api.example.com`）

## 数据结构变化

### 变量配置数据结构 (variables_config.json)

```json
{
  "name": "api_url",
  "type": "fixed",
  "value": "https://test-api.example.com",
  "environment": "测试环境",
  "project": "项目A",
  "description": "API基础地址"
}
```

### 唯一性约束

同一个变量名在以下情况下被视为不同的变量：
- 环境不同
- 项目不同
- 环境和项目的组合不同

例如，以下都是独立的变量：
- `api_url` + `测试环境` + `项目A`
- `api_url` + `UAT环境` + `项目A`
- `api_url` + `测试环境` + `项目B`
- `api_url` + `测试环境` + `不限项目`

## 注意事项

1. **删除变量**：删除变量时会根据环境和项目精确删除，不会影响其他环境或项目的同名变量
2. **编辑变量**：编辑变量时，会更新当前环境和项目组合下的变量值
3. **变量优先级**：执行用例时，会优先使用环境+项目精确匹配的变量
4. **向后兼容**：旧的变量数据会被视为"全局"环境和"不限"项目的变量
