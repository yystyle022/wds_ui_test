# Flask + Vue 自动化测试平台 - 项目架构文档

## 一、项目概述

### 1.1 项目简介
这是一个基于 Flask + Vue3 的 Web 自动化测试平台，支持可视化管理测试用例、元素、变量、任务等，并基于 Playwright 实现浏览器自动化测试执行。

### 1.2 技术栈

**后端技术栈：**
- Python 3.x
- Flask (Web框架)
- Flask-CORS (跨域支持)
- Playwright (浏览器自动化)
- Asyncio (异步执行)
- PyMySQL / psycopg2 (数据库支持)
- OpenCV / NumPy (图像处理，滑块验证)

**前端技术栈：**
- Vue 3.5+
- TypeScript
- Vue Router 4.x (路由管理)
- Naive UI 2.x (UI组件库)
- Axios (HTTP客户端)
- ECharts 5.x (数据可视化)
- Vite 6.x (构建工具)
- VueUse (组合式工具库)

### 1.3 项目结构

```
flask_vue/
├── backend/                    # 后端代码
│   ├── app.py                 # Flask主应用（API路由、中间件、业务逻辑）
│   ├── test_executor.py       # 测试执行器（Playwright封装）
│   ├── screenshots/           # 测试截图存储目录
│   ├── *.json                 # 数据存储文件
│   │   ├── data.json         # 测试用例数据
│   │   ├── elements.json     # 元素库数据
│   │   ├── reports.json      # 测试报告数据
│   │   ├── tasks.json        # 任务配置数据
│   │   ├── variables.json    # 运行时变量数据
│   │   ├── variables_config.json  # 变量配置数据
│   │   ├── projects.json     # 项目管理数据
│   │   ├── environments.json # 环境管理数据
│   │   └── databases.json    # 数据库配置数据
│   ├── settings.json          # 系统配置
│   └── app.log               # 应用日志
│
├── frontend/                  # 前端代码
│   ├── src/
│   │   ├── components/       # Vue组件
│   │   │   ├── Dashboard.vue           # 仪表盘（首页）
│   │   │   ├── DataTableComponent.vue  # 用例列表
│   │   │   ├── ElementList.vue         # 元素列表
│   │   │   ├── ReportList.vue          # 报告列表
│   │   │   ├── ReportView.vue          # 报告详情
│   │   │   ├── ProjectList.vue         # 任务列表
│   │   │   ├── VariableList.vue        # 变量管理
│   │   │   ├── ProjectManage.vue       # 项目管理
│   │   │   ├── EnvironmentManage.vue   # 环境管理
│   │   │   ├── DatabaseManage.vue      # 数据管理
│   │   │   ├── LogViewer.vue           # 日志查询
│   │   │   └── SettingsModal.vue       # 设置弹窗
│   │   ├── router/           # 路由配置
│   │   │   └── index.ts
│   │   ├── assets/           # 静态资源
│   │   ├── App.vue           # 根组件
│   │   └── main.ts           # 应用入口
│   ├── public/               # 公共资源
│   ├── package.json          # 前端依赖
│   └── vite.config.ts        # Vite配置
│
└── venv/                      # Python虚拟环境

```

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (Vue3 + Naive UI)               │
│  ┌──────────┬──────────┬──────────┬──────────┬────────┐ │
│  │ 仪表盘   │ 用例管理  │ 元素管理  │ 报告管理  │ 设置   │ │
│  └──────────┴──────────┴──────────┴──────────┴────────┘ │
└─────────────────────────────────────────────────────────┘
                            ↕ HTTP/REST API
┌─────────────────────────────────────────────────────────┐
│                  后端层 (Flask + CORS)                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │  API路由层 (RESTful接口)                          │   │
│  │  - 用例CRUD  - 元素CRUD  - 报告CRUD              │   │
│  │  - 任务管理  - 变量管理  - 项目/环境/数据库管理    │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  业务逻辑层                                        │   │
│  │  - 测试执行调度  - 报告生成  - 配置管理          │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  测试执行引擎 (test_executor.py)                  │   │
│  │  - Playwright封装  - 异步执行  - 并行/串行       │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────┐
│              数据存储层 (JSON文件 + 数据库)               │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │ 用例数据  │ 元素数据  │ 报告数据  │ 配置数据  │         │
│  └──────────┴──────────┴──────────┴──────────┘         │
│  ┌──────────────────────────────────────────┐           │
│  │  外部数据库 (MySQL/PostgreSQL - 变量查询) │           │
│  └──────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────┐
│              浏览器自动化层 (Playwright)                  │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │ Chrome   │ Firefox  │ Edge     │ Chromium │         │
│  └──────────┴──────────┴──────────┴──────────┘         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 数据流设计

**测试执行流程：**
```
用户点击执行 → 前端发送请求 → 后端创建临时报告(executing状态)
→ test_executor初始化浏览器 → 执行用例步骤 → 收集执行结果
→ 生成最终报告 → 保存到reports.json → 返回前端展示
```

**变量解析流程：**
```
步骤中的变量引用(${varName}) → 从variables.json读取
→ 根据变量类型解析：
   - fixed: 直接返回配置的值
   - random: 生成随机数
   - database: 查询数据库
   - auth: 注入Authorization header
→ 替换到实际值 → 执行操作
```

---

## 三、核心模块详解

### 3.1 后端核心模块

#### 3.1.1 Flask应用 (app.py)

**主要职责：**
- RESTful API 路由定义
- 请求/响应日志记录
- 跨域(CORS)支持
- 文件存储管理
- 业务逻辑协调

**核心功能模块：**

```python
# 1. 请求日志中间件
@app.before_request
def log_request_info()
    # 记录请求ID、头信息、请求体、查询参数

@app.after_request
def log_response_info(response)
    # 记录响应状态、响应头、响应体

# 2. 测试用例管理
GET    /api/data                # 获取用例列表（支持过滤）
POST   /api/add_case            # 添加用例
GET    /api/testcase/<id>       # 获取用例详情
PUT    /api/testcase/<id>       # 更新用例
DELETE /api/testcase/<id>       # 删除用例

# 3. 测试执行
POST   /api/execute             # 执行单个用例
POST   /api/batch_execute       # 批量执行用例

# 4. 报告管理
GET    /api/reports             # 获取报告列表
GET    /api/reports/<id>        # 获取报告详情
DELETE /api/reports/<id>        # 删除报告
DELETE /api/reports/batch       # 批量删除报告

# 5. 元素管理
GET    /api/elements            # 获取元素列表
POST   /api/elements            # 添加元素
PUT    /api/elements/<id>       # 更新元素
DELETE /api/elements/<id>       # 删除元素

# 6. 任务管理
GET    /api/tasks               # 获取任务列表
POST   /api/tasks               # 创建任务
PUT    /api/tasks/<id>          # 更新任务
DELETE /api/tasks/<id>          # 删除任务
POST   /api/tasks/<id>/execute  # 执行任务

# 7. 变量管理
GET    /api/variables/list      # 获取变量配置列表
POST   /api/variables/add       # 添加变量配置
DELETE /api/variables/<name>    # 删除变量
GET    /api/variables/generate/<name>  # 生成变量值

# 8. 项目/环境/数据库管理
GET/POST/PUT/DELETE /api/projects          # 项目CRUD
GET/POST/PUT/DELETE /api/environments      # 环境CRUD
GET/POST/PUT/DELETE /api/databases         # 数据库配置CRUD
POST /api/databases/test                   # 测试数据库连接

# 9. 系统配置
GET    /api/settings            # 获取系统设置
POST   /api/settings            # 更新系统设置
GET    /api/logs                # 获取日志
POST   /api/logs/clear          # 清空日志
```

**关键实现要点：**

1. **自动创建元素功能**
```python
def auto_create_missing_elements(testcase_steps, project, module):
    """
    遍历用例步骤，如果步骤中有xpath但element_id不存在，
    自动创建元素并更新element_id
    """
```

2. **Authorization动态注入**
```python
def determine_auth_profiles(testcase):
    """
    根据用例的authMode(enable/disable/auto)和
    所属项目/环境，决定是否注入Authorization
    """
```

3. **执行状态管理**
```python
# 创建executing状态的临时报告
temp_report = {
    "status": "executing",
    "id": temp_report_id,
    ...
}
save_report(temp_report)  # 立即保存

# 执行完成后用相同ID覆盖
final_report["id"] = temp_report_id
save_report(final_report)
```

#### 3.1.2 测试执行器 (test_executor.py)

**类结构：**
```python
class TestExecutor:
    """测试执行器 - Playwright封装"""
    
    async def initialize(...)       # 初始化浏览器
    async def close()               # 关闭浏览器资源
    
    # 页面操作方法
    async def open_url(url)         # 打开URL，动态注入Authorization
    async def click_element(xpath)  # 点击元素
    async def input_text(xpath, text)  # 输入文本
    async def wait_time(seconds)    # 等待指定时间
    async def slide_captcha()       # 滑块验证
    async def check_element_exists(xpath)  # 检查元素是否存在
    async def get_element_text(xpath)      # 获取元素文本
    async def verify_element_value(xpath, expected)  # 验证元素值
    
    # 变量管理
    def load_variables_from_file()  # 从文件加载变量
    def save_variables_to_file()    # 保存变量到文件
    def set_variable(name, value)   # 设置变量
    def get_variable(name)          # 获取变量
    def resolve_variable(text)      # 解析变量引用 ${varName}
    
    # 数据库查询变量
    async def query_database_variable(var_config)  # 查询数据库获取变量值

# Cookie管理类
class CookieManager:
    """官网Cookie管理"""
    async def login_and_save_cookie(login_config)

class AdminCookieManager:
    """管理端Cookie管理"""
    async def login_and_save_cookie(login_config)
```

**核心功能实现：**

1. **浏览器初始化**
```python
async def initialize(self, browser_name="chrome", headless=False, 
                     save_screenshots=False, auth_profiles=None):
    """
    - 支持Chrome/Firefox/Edge/Chromium
    - 设置viewport为1920x1080
    - 配置Authorization profiles（域名→token映射）
    - 设置控制台和错误监听
    """
```

2. **动态Authorization注入**
```python
async def open_url(self, url: str):
    """
    打开URL前，根据URL的域名匹配auth_profiles，
    动态设置对应的Authorization header
    """
    domain = extract_domain(url)
    for profile in self.auth_profiles:
        if domain in profile.get("domains", []):
            await self.context.set_extra_http_headers({
                "Authorization": profile["token"]
            })
            break
```

3. **变量解析**
```python
def resolve_variable(self, text: str) -> str:
    """
    解析文本中的变量引用：${varName}
    支持嵌套解析：${base_url}/api/${version}
    """
    pattern = r'\$\{([^}]+)\}'
    while re.search(pattern, text):
        text = re.sub(pattern, lambda m: self.get_variable(m.group(1)), text)
    return text
```

4. **滑块验证算法**
```python
async def slide_captcha(self):
    """
    1. 下载背景图和滑块图
    2. 使用OpenCV模板匹配计算滑动距离
    3. 生成模拟人类的滑动轨迹
    4. 执行拖拽操作
    """
    # 模板匹配
    result = cv2.matchTemplate(bg_img, slider_img, cv2.TM_CCOEFF_NORMED)
    distance = np.where(result == np.max(result))[1][0]
    
    # 生成轨迹
    tracks = get_slide_locus(distance)
    
    # 拖拽滑块
    for track in tracks:
        await slider.mouse.move(x + track, y)
```

5. **数据库变量查询**
```python
async def query_database_variable(self, var_config):
    """
    根据变量配置中的数据库名称和SQL查询，
    从databases.json找到对应配置，
    执行查询并返回结果
    支持MySQL和PostgreSQL
    """
```

6. **截图保存**
```python
async def take_screenshot(self, step_desc=""):
    """
    如果启用save_screenshots：
    1. 生成唯一文件名（timestamp_random.png）
    2. 保存到screenshots/目录
    3. 返回相对路径用于报告展示
    """
```

**测试执行流程函数：**

```python
async def execute_test_case(testcase, config):
    """
    执行单个测试用例
    1. 初始化TestExecutor
    2. 逐步执行testcase_step
    3. 记录每步结果（pass/fail/skip）
    4. 生成测试报告
    5. 关闭浏览器
    """

async def execute_batch_test_cases(testcases, config):
    """
    顺序执行多个用例
    - 复用同一个浏览器实例
    - 汇总生成批量报告
    """

async def execute_batch_test_cases_parallel(testcases, config):
    """
    并行执行多个用例
    - 使用ThreadPoolExecutor多线程
    - 每个用例独立浏览器实例
    - 汇总生成批量报告
    """
```

**支持的操作类型：**
```python
OPERATION_MAPPING = {
    "open_url": "打开网址",
    "click": "点击元素",
    "click_text": "通过文本点击",
    "input": "输入内容",
    "wait": "等待时长",
    "slide_captcha": "滑动滑块验证",
    "login_website": "登录官网",
    "check_element_exists": "检查元素是否存在",
    "check_element_not_exists": "检查元素不存在",
    "get_element_text": "获取元素的文案",
    "verify_element_value": "验证元素的值",
    "verify_variable_value": "验证获取的变量值",
}
```

---

### 3.2 前端核心模块

#### 3.2.1 路由配置 (router/index.ts)

```typescript
const routes = [
  { path: "/", redirect: "/dashboard" },
  { path: "/dashboard", component: Dashboard },              // 仪表盘
  { path: "/test-cases", component: DataTableComponent },    // 用例列表
  { path: "/elements", component: ElementList },             // 元素列表
  { path: "/reports", component: ReportList },               // 报告列表
  { path: "/reports/:id", component: ReportView },           // 报告详情
  { path: "/projects", component: ProjectList },             // 任务列表
  { path: "/variables", component: VariableList },           // 变量管理
  { path: "/project-management", component: ProjectManage }, // 项目管理
  { path: "/env-management", component: EnvironmentManage }, // 环境管理
  { path: "/database-management", component: DatabaseManage },// 数据管理
  { path: "/logs", component: LogViewer },                   // 日志查询
];
```

#### 3.2.2 核心组件

**1. App.vue - 应用入口**
```vue
<template>
  <n-layout has-sider>
    <!-- 左侧导航栏：可折叠，带Logo和标题 -->
    <n-layout-sider>
      <n-menu :options="menuOptions" />
    </n-layout-sider>
    
    <!-- 右侧内容区：路由视图 -->
    <n-layout-content>
      <router-view />
    </n-layout-content>
  </n-layout>
</template>
```

**功能：**
- 全局布局框架
- 侧边栏导航（支持折叠）
- 路由监听与菜单激活状态同步
- Naive UI全局配置

**2. Dashboard.vue - 仪表盘**

**展示内容：**
- 总用例数统计
- 按项目统计用例数量
- 用例类型分布饼图（ECharts）
- 用例模块分布饼图（ECharts）
- 最近测试报告列表（最近3条）

**数据源：**
```typescript
// API: GET /api/dashboard
{
  total_cases: number,
  project_stats: Array<{id, name, count}>,
  type_distribution: Array<{name, value}>,
  module_distribution: Array<{name, value}>,
  recent_reports: Array<Report>
}
```

**3. DataTableComponent.vue - 用例列表**

**核心功能：**
- 用例列表展示（表格）
- 搜索过滤（项目、模块、名称、类型、环境）
- 用例CRUD操作
- 单个/批量执行用例
- 执行配置弹窗
  - 浏览器选择（Chrome/Firefox/Edge）
  - 无头模式开关
  - 保存截图开关
  - 多线程并行开关
  - 报告名称和描述

**用例编辑表单：**
```typescript
{
  name: string,          // 用例名称
  module: string,        // 所属模块
  type: string,          // 用例类型
  project: string,       // 所属项目
  environment: string,   // 运行环境
  authMode: string,      // Authorization模式：auto/enable/disable
  testcase_step: Array<{ // 测试步骤
    describe: string,
    operate: string,
    xpath: string,
    element_id: number,
    element_name: string,
    input_value: string,
    expected_value: string
  }>
}
```

**4. ElementList.vue - 元素列表**

**功能：**
- 元素库展示
- 按项目/模块/元素名搜索
- 元素CRUD操作

**元素数据结构：**
```typescript
{
  id: number,
  project: string,
  module: string,
  elementName: string,
  xpath: string,
  description: string
}
```

**5. ReportList.vue - 报告列表**

**功能：**
- 报告列表展示
- 状态标签（executing/success/fail）
- 通过率展示
- 查看详情
- 删除报告（单个/批量）

**6. ReportView.vue - 报告详情**

**展示内容：**
- 报告基本信息（名称、时间、状态、通过率、耗时）
- 执行配置信息
- 执行用例列表
- 步骤详情表格
  - 步骤描述
  - 操作类型
  - 执行结果（pass/fail/skip）
  - 详细信息（错误信息/截图）
- 截图预览（支持点击放大）

**7. VariableList.vue - 变量管理**

**变量类型：**
- **fixed（固定值）**：直接配置固定值
- **random（随机数）**：配置范围，运行时生成
- **database（数据库查询）**：配置数据库和SQL
- **auth（Authorization）**：配置token和匹配域名

**功能：**
- 变量配置CRUD
- 按环境和项目分类
- 变量值生成预览
- 支持在步骤中引用：`${varName}`

**8. ProjectManage.vue - 项目管理**

**功能：**
- 项目CRUD
- 项目描述管理
- 用例按项目分类

**9. EnvironmentManage.vue - 环境管理**

**功能：**
- 环境CRUD（测试/预发/生产等）
- 环境变量管理（每个环境独立的key-value配置）

**10. DatabaseManage.vue - 数据管理**

**功能：**
- 数据库配置CRUD
- 支持MySQL和PostgreSQL
- 测试连接功能
- 按项目和环境分类

**配置项：**
```typescript
{
  name: string,
  type: "mysql" | "postgresql",
  host: string,
  port: number,
  username: string,
  password: string,
  database: string,
  project: string,
  environment: string,
  description: string
}
```

**11. ProjectList.vue - 任务列表**

**功能：**
- 任务CRUD
- 任务配置
  - 任务名称
  - 所属项目
  - 执行类型（立即执行/定时执行）
  - 定时时间配置
  - 选择用例列表
- 执行任务

**12. LogViewer.vue - 日志查询**

**功能：**
- 日志实时查看（最新在前）
- 按级别过滤（INFO/WARNING/ERROR）
- 关键词搜索
- 显示行数配置（默认500行）
- 清空日志
- 自动滚动到底部

**13. SettingsModal.vue - 设置弹窗**

**配置项：**
```typescript
{
  defaultBrowser: "chrome" | "firefox" | "chromium" | "msedge",
  defaultHeadless: boolean,
  saveScreenshots: boolean,
  enableMultiThread: boolean
}
```

---

## 四、数据模型设计

### 4.1 测试用例 (data.json)

```json
{
  "id": 1,
  "name": "用例名称",
  "module": "模块名",
  "type": "功能测试",
  "project": "项目名",
  "environment": "测试环境",
  "authMode": "auto",
  "testcase_step": [
    {
      "describe": "步骤描述",
      "operate": "操作类型",
      "xpath": "元素xpath",
      "element_id": 1,
      "element_name": "元素名称",
      "input_value": "输入值或变量${var}",
      "expected_value": "期望值"
    }
  ]
}
```

### 4.2 元素库 (elements.json)

```json
{
  "id": 1,
  "project": "项目名",
  "module": "模块名",
  "elementName": "元素名称",
  "xpath": "//button[@id='submit']",
  "description": "描述信息"
}
```

### 4.3 测试报告 (reports.json)

```json
{
  "id": 1234567890,
  "test_id": 1,
  "name": "报告名称",
  "time": "2025-01-01 12:00:00",
  "status": "success | fail | executing",
  "pass_rate": 95.5,
  "duration": "00:02:30",
  "description": "报告描述",
  "config": {
    "browser": "chrome",
    "headless": false,
    "saveScreenshots": true,
    "enableMultiThread": false
  },
  "executed_cases": [
    {"id": 1, "name": "用例1"},
    {"id": 2, "name": "用例2"}
  ],
  "steps": [
    {
      "describe": "步骤描述",
      "action": "操作类型",
      "result": "pass | fail | skip",
      "details": {
        "error": "错误信息",
        "screenshot": "screenshots/xxx.png"
      }
    }
  ]
}
```

### 4.4 变量配置 (variables_config.json)

```json
{
  "name": "变量名",
  "type": "fixed | random | database | auth",
  "description": "描述",
  "environment": "测试环境",
  "project": "项目名",
  
  // type=fixed
  "value": "固定值",
  
  // type=random
  "minValue": 1,
  "maxValue": 100,
  
  // type=database
  "database": "数据库配置名",
  "query": "SELECT value FROM table",
  
  // type=auth
  "value": "Bearer token_xxx",
  "matchDomains": ["api.example.com", "admin.example.com"]
}
```

### 4.5 项目配置 (projects.json)

```json
{
  "id": 1,
  "name": "项目名称",
  "description": "项目描述",
  "createTime": "2025-01-01 12:00:00"
}
```

### 4.6 环境配置 (environments.json)

```json
{
  "id": 1,
  "name": "测试环境",
  "description": "环境描述",
  "variables": [
    {
      "key": "base_url",
      "value": "https://test.example.com",
      "description": "基础URL"
    }
  ],
  "createTime": "2025-01-01 12:00:00"
}
```

### 4.7 数据库配置 (databases.json)

```json
{
  "id": 1,
  "name": "配置名称",
  "type": "mysql",
  "host": "localhost",
  "port": "3306",
  "username": "root",
  "password": "password",
  "database": "test_db",
  "project": "项目名",
  "environment": "测试环境",
  "description": "描述",
  "createTime": "2025-01-01 12:00:00"
}
```

### 4.8 任务配置 (tasks.json)

```json
{
  "id": 1,
  "name": "任务名称",
  "description": "任务描述",
  "project": "项目名",
  "executionType": "immediate | scheduled",
  "scheduleTime": "2025-01-01 12:00:00",
  "selectedCases": [1, 2, 3],
  "createTime": "2025-01-01 12:00:00",
  "status": "active"
}
```

---

## 五、核心功能实现要点

### 5.1 变量系统

**设计目标：**
- 支持多种变量类型（固定值、随机数、数据库查询、Authorization）
- 支持按项目和环境隔离
- 支持在步骤中动态引用和解析

**实现机制：**

1. **变量配置存储** (variables_config.json)
   - 管理变量的元数据（类型、范围、配置）
   - 前端通过VariableList.vue进行可视化管理

2. **变量运行时存储** (variables.json)
   - 存储执行过程中生成的变量值
   - test_executor在执行时读取和更新

3. **变量解析流程**
```python
# 步骤1: 解析步骤中的变量引用
input_value = "${base_url}/api/${version}"

# 步骤2: 递归替换所有 ${varName}
resolved_value = executor.resolve_variable(input_value)
# 结果: "https://test.example.com/api/v2"

# 步骤3: 执行实际操作
await executor.input_text(xpath, resolved_value)
```

4. **数据库变量实现**
```python
async def query_database_variable(var_config):
    # 1. 从databases.json获取数据库配置
    db_config = get_database_config(var_config["database"])
    
    # 2. 建立数据库连接
    if db_config["type"] == "mysql":
        conn = pymysql.connect(...)
    elif db_config["type"] == "postgresql":
        conn = psycopg2.connect(...)
    
    # 3. 执行查询
    cursor.execute(var_config["query"])
    result = cursor.fetchone()[0]
    
    # 4. 保存到运行时变量
    self.set_variable(var_name, result)
    return result
```

5. **Authorization变量实现**
```python
def resolve_auth_profiles(project, environment):
    """
    从variables_config.json中筛选：
    - type == "auth"
    - project匹配或为空（全局）
    - environment匹配或为"全局"
    返回: [{"name", "domains": [], "token": "xxx"}]
    """
    
# 在open_url时动态注入
async def open_url(url):
    domain = extract_domain(url)
    for profile in self.auth_profiles:
        if domain in profile["domains"]:
            await self.context.set_extra_http_headers({
                "Authorization": profile["token"]
            })
```

### 5.2 元素自动创建机制

**问题场景：**
用户在编辑用例步骤时，可能直接输入xpath而未先创建元素

**解决方案：**
```python
def auto_create_missing_elements(testcase_steps, project, module):
    """
    在保存/更新用例时自动触发：
    1. 遍历所有步骤
    2. 如果步骤有xpath但element_id无效
    3. 自动创建元素：
       - 生成新ID
       - 元素名称优先使用步骤中的element_name，其次使用describe
       - xpath使用步骤中的xpath
       - 更新步骤的element_id
    4. 保存到elements.json
    """
```

**触发时机：**
- POST /api/add_case
- PUT /api/testcase/<id>

### 5.3 测试执行状态管理

**需求：**
- 用户点击执行后立即看到"执行中"状态
- 执行完成后自动更新为最终状态
- 避免用户重复点击

**实现方案：**

```python
# 步骤1: 创建临时报告（状态=executing）
temp_report_id = int(time.time())
executing_report = {
    "id": temp_report_id,
    "status": "executing",
    "pass_rate": 0,
    "steps": [],
    ...
}
save_report(executing_report)  # 立即保存

# 步骤2: 执行测试
try:
    report = await execute_test_case(testcase, config)
    report["id"] = temp_report_id  # 复用相同ID
    save_report(report)  # 覆盖临时报告
except Exception as e:
    error_report = {
        "id": temp_report_id,
        "status": "fail",
        "error": str(e),
        ...
    }
    save_report(error_report)
```

**前端轮询：**
- 报告列表会定时刷新
- 执行中的报告显示进度动画
- 完成后自动更新状态标签

### 5.4 并行执行实现

**顺序执行** (execute_batch_test_cases)
```python
async def execute_batch_test_cases(testcases, config):
    """
    优点：复用浏览器，节省资源
    缺点：执行慢，一个失败可能影响后续
    """
    executor = TestExecutor()
    await executor.initialize(...)
    
    all_steps = []
    for testcase in testcases:
        steps = await execute_single_case_steps(executor, testcase)
        all_steps.extend(steps)
    
    await executor.close()
    return generate_batch_report(all_steps)
```

**并行执行** (execute_batch_test_cases_parallel)
```python
async def execute_batch_test_cases_parallel(testcases, config):
    """
    优点：快速执行，用例间隔离
    缺点：资源消耗大
    """
    with concurrent.futures.ThreadPoolExecutor(max_workers=5) as executor:
        futures = []
        for testcase in testcases:
            future = executor.submit(
                asyncio.run, 
                execute_test_case(testcase, config)
            )
            futures.append(future)
        
        reports = [f.result() for f in futures]
    
    return merge_batch_reports(reports)
```

### 5.5 滑块验证实现

**技术方案：**
- OpenCV模板匹配算法
- 模拟人类滑动轨迹

**实现步骤：**

```python
async def slide_captcha(self):
    # 1. 切换到iframe
    frame = page.frame_locator("//iframe[@id='tcaptcha_iframe']")
    
    # 2. 下载背景图和滑块图
    await download_images(page, "bg", frame_xpath, bg_xpath, "./temp")
    await download_images(page, "slider", frame_xpath, slider_xpath, "./temp")
    
    # 3. 使用OpenCV计算滑动距离
    bg_img = cv2.imread("./temp/bg.jpg", 0)
    slider_img = cv2.imread("./temp/slider.jpg", 0)
    result = cv2.matchTemplate(bg_img, slider_img, cv2.TM_CCOEFF_NORMED)
    distance = np.where(result == np.max(result))[1][0]
    
    # 4. 生成滑动轨迹（模拟加速→减速）
    tracks = get_slide_locus(distance)
    # 返回: [2, 4, 6, 5, 3, 2, 1, ...]  # 像素增量
    
    # 5. 执行拖拽
    slider = frame.locator("//div[@id='tcaptcha_drag_thumb']")
    await slider.hover()
    await page.mouse.down()
    
    for track in tracks:
        await page.mouse.move(x + track, y)
        await asyncio.sleep(random.uniform(0.01, 0.03))
    
    await page.mouse.up()
```

**轨迹生成算法：**
```python
def get_slide_locus(distance):
    """
    模拟人类滑动：
    - 前4/5路程：加速（a=2）
    - 后1/5路程：减速（a=-3）
    使用物理公式：s = v0*t + 0.5*a*t²
    """
    tracks = []
    v = 0
    current = 0
    mid = distance * 4 / 5
    
    while current <= distance:
        a = 2 if current < mid else -3
        s = v * 0.312 + 0.5 * a * (0.312**2)
        current += s
        tracks.append(round(s))
        v = v + a * 0.312
    
    return tracks
```

### 5.6 日志记录系统

**设计目标：**
- 全面记录请求/响应
- 方便问题排查
- 支持前端查询

**实现机制：**

1. **Flask中间件**
```python
@app.before_request
def log_request_info():
    request_id = str(uuid.uuid4())[:8]
    logger.info(f"[{request_id}] {request.method} {request.path}")
    logger.info(f"[{request_id}] Headers: {dict(request.headers)}")
    logger.info(f"[{request_id}] Body: {request.get_json()}")

@app.after_request
def log_response_info(response):
    request_id = getattr(request, "request_id", "unknown")
    logger.info(f"[{request_id}] Status: {response.status}")
    logger.info(f"[{request_id}] Response: {response.get_json()}")
```

2. **日志配置**
```python
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] [%(name)s] - %(message)s",
    handlers=[
        logging.StreamHandler(),           # 输出到控制台
        logging.FileHandler("app.log")     # 输出到文件
    ]
)
```

3. **前端日志查询**
```typescript
// API: GET /api/logs?lines=500&level=INFO&search=关键词
// 返回最新N行，支持级别过滤和关键词搜索
// 日志倒序显示（最新在前）
```

### 5.7 截图管理

**实现机制：**

1. **截图保存**
```python
async def take_screenshot(step_desc=""):
    if not self.save_screenshots:
        return None
    
    filename = f"{int(time.time())}_{random.randint(1000,9999)}.png"
    filepath = os.path.join("screenshots", filename)
    await self.page.screenshot(path=filepath, full_page=True)
    return filepath  # 返回相对路径
```

2. **报告中引用**
```json
{
  "steps": [
    {
      "describe": "步骤描述",
      "result": "pass",
      "details": {
        "screenshot": "screenshots/1234567890_1234.png"
      }
    }
  ]
}
```

3. **前端展示**
```vue
<template>
  <n-image 
    :src="`http://localhost:8081/${step.details.screenshot}`"
    preview
  />
</template>
```

4. **删除报告时清理截图**
```python
@app.route("/api/reports/<int:report_id>", methods=["DELETE"])
def delete_report(report_id):
    # 1. 读取报告
    # 2. 遍历steps提取所有screenshot路径
    # 3. 删除物理文件
    for step in report["steps"]:
        screenshot = step.get("details", {}).get("screenshot")
        if screenshot and os.path.exists(screenshot):
            os.remove(screenshot)
    # 4. 删除报告记录
```

### 5.8 Cookie管理

**使用场景：**
- 登录官网获取Cookie用于后续测试
- 登录管理端获取Cookie

**实现机制：**

```python
class CookieManager:
    async def login_and_save_cookie(self, login_config):
        """
        1. 启动浏览器
        2. 访问登录页
        3. 输入用户名密码
        4. 点击登录
        5. 等待登录成功
        6. 保存Cookie到文件（cookie/<website>_cookie.json）
        """
        playwright = await async_playwright().start()
        browser = await playwright.chromium.launch(headless=False)
        context = await browser.new_context()
        page = await context.new_page()
        
        await page.goto(login_config["website_url"])
        # ... 登录操作
        
        cookies = await context.cookies()
        cookie_file = os.path.join(COOKIE_DIR, f"{website}_cookie.json")
        with open(cookie_file, "w") as f:
            json.dump(cookies, f)
        
        await browser.close()
```

**API接口：**
```python
POST /api/login-for-cookie          # 官网登录
POST /api/login-for-admin-cookie    # 管理端登录
```

---

## 六、数据流示例

### 6.1 用例执行完整流程

```
1. 用户点击"执行"按钮
   ↓
2. 前端发送 POST /api/execute
   Body: { id: 1, config: {...} }
   ↓
3. 后端创建临时报告（status=executing）
   报告ID: 1234567890
   立即保存到 reports.json
   ↓
4. 返回前端 { report: {...} }
   前端显示"执行中"状态
   ↓
5. 后端异步执行测试
   TestExecutor.initialize()
   ↓
6. 逐步执行 testcase_step
   对每个步骤：
   - 解析变量: ${base_url} → https://test.com
   - 查找元素: element_id → xpath
   - 执行操作: click/input/wait/...
   - 记录结果: pass/fail
   - 截图（如果开启）
   ↓
7. 生成最终报告
   计算通过率、总耗时
   汇总所有步骤结果
   ↓
8. 覆盖临时报告（相同ID）
   status: executing → success/fail
   保存到 reports.json
   ↓
9. 前端定时刷新报告列表
   显示最终状态和通过率
```

### 6.2 变量解析流程

```
用例步骤: 
{
  "operate": "open_url",
  "input_value": "${base_url}/api/users"
}
   ↓
1. 读取 variables_config.json
   找到变量配置: base_url
   {
     "name": "base_url",
     "type": "fixed",
     "value": "https://test.example.com",
     "environment": "测试环境"
   }
   ↓
2. 根据type生成值
   type=fixed: 直接返回 value
   type=random: random.randint(minValue, maxValue)
   type=database: 查询数据库
   type=auth: 返回 "已配置(Authorization)"
   ↓
3. 替换变量引用
   "${base_url}/api/users"
   → "https://test.example.com/api/users"
   ↓
4. 执行操作
   await executor.open_url("https://test.example.com/api/users")
```

### 6.3 Authorization注入流程

```
用例配置:
{
  "project": "项目A",
  "environment": "测试环境",
  "authMode": "auto"  // 或 enable/disable
}
   ↓
1. 解析authMode
   - disable: 直接返回空数组
   - enable: 强制使用
   - auto: 检查是否有登录步骤，有则不用
   ↓
2. 从 variables_config.json 筛选
   条件: type="auth" 
         AND (project匹配 OR project为空)
         AND (environment匹配 OR environment="全局")
   ↓
3. 获得 auth_profiles
   [
     {
       "name": "API认证",
       "domains": ["api.example.com", "admin.example.com"],
       "token": "Bearer xxx"
     }
   ]
   ↓
4. 传入 TestExecutor
   executor.set_auth_profiles(auth_profiles)
   ↓
5. 执行 open_url 时匹配域名
   URL: https://api.example.com/users
   提取域名: api.example.com
   匹配成功 → 注入 Authorization header
   ↓
6. 设置请求头
   await context.set_extra_http_headers({
     "Authorization": "Bearer xxx"
   })
```

---

## 七、部署运维

### 7.1 环境要求

**后端环境：**
```bash
Python 3.8+
pip install flask flask-cors playwright pymysql psycopg2-binary opencv-python numpy requests

# 安装Playwright浏览器
playwright install
```

**前端环境：**
```bash
Node.js 16+
cd frontend
npm install
```

### 7.2 启动方式

**开发环境：**

```bash
# 后端（端口8081）
cd backend
python app.py

# 前端（端口5173）
cd frontend
npm run dev
```

**生产环境：**

```bash
# 构建前端
cd frontend
npm run build
# 生成 dist/ 目录

# 启动后端（自动服务前端静态文件）
cd backend
python app.py
# 访问 http://localhost:8081
```

### 7.3 配置说明

**系统配置文件** (settings.json)
```json
{
  "defaultBrowser": "chrome",
  "defaultHeadless": false,
  "saveScreenshots": false,
  "enableMultiThread": false
}
```

**环境变量：**
```bash
PORT=8081  # 后端端口（默认8081）
```

### 7.4 日志管理

**日志文件位置：**
- `backend/app.log` - 应用主日志
- `backend/server.log` - 服务器日志（如有）

**日志级别：**
- INFO: 正常请求、操作记录
- WARNING: 警告信息
- ERROR: 错误信息

**日志查看：**
- 前端：访问"日志查询"页面
- 后端：直接查看 app.log 文件

---

## 八、扩展性设计

### 8.1 新增操作类型

**步骤：**

1. 在 `test_executor.py` 添加方法
```python
async def my_new_operation(self, params):
    """新操作的实现"""
    pass
```

2. 在 `OPERATION_MAPPING` 添加映射
```python
OPERATION_MAPPING = {
    ...
    "my_operation": "我的新操作"
}
```

3. 在 `execute_step` 中添加分支
```python
if operate == "my_operation":
    result = await self.my_new_operation(params)
```

4. 前端添加操作选项
```typescript
// DataTableComponent.vue
const operationOptions = [
  ...
  { label: "我的新操作", value: "my_operation" }
]
```

### 8.2 新增变量类型

**步骤：**

1. 在 `variables_config.json` 定义新类型配置结构
2. 在 `test_executor.py` 实现变量解析逻辑
```python
async def resolve_new_type_variable(self, var_config):
    # 实现新类型的变量获取逻辑
    pass
```

3. 在 `VariableList.vue` 添加表单配置
```vue
<n-form-item v-if="formData.type === 'new_type'">
  <!-- 新类型的配置表单 -->
</n-form-item>
```

### 8.3 新增数据库类型

**步骤：**

1. 安装对应数据库驱动
```bash
pip install <database-driver>
```

2. 在 `app.py` 的 `try_connect_database` 添加连接逻辑
```python
elif db_type == "mongodb":
    import pymongo
    client = pymongo.MongoClient(...)
```

3. 在 `test_executor.py` 的 `query_database_variable` 添加查询逻辑
```python
elif db_config["type"] == "mongodb":
    # MongoDB查询实现
    pass
```

4. 前端添加类型选项
```typescript
// DatabaseManage.vue
const typeOptions = [
  { label: "MongoDB", value: "mongodb" }
]
```

---

## 九、常见问题与解决方案

### 9.1 Playwright相关

**问题1: 浏览器启动失败**
```
解决：检查Playwright是否正确安装
playwright install
```

**问题2: 元素定位失败**
```
解决：
1. 检查xpath是否正确
2. 增加wait时间
3. 使用截图功能查看页面状态
```

**问题3: 无头模式截图不完整**
```
解决：在initialize时设置viewport大小
viewport={"width": 1920, "height": 1080}
```

### 9.2 变量相关

**问题1: 变量未解析**
```
解决：
1. 确认变量名拼写正确
2. 检查variables_config.json是否存在该变量
3. 确认项目和环境匹配
```

**问题2: 数据库变量查询失败**
```
解决：
1. 测试数据库连接
2. 检查SQL语法
3. 确认数据库配置的项目/环境匹配
```

### 9.3 执行相关

**问题1: 并行执行报错**
```
解决：
1. 降低并发数（修改ThreadPoolExecutor的max_workers）
2. 检查系统资源是否充足
3. 改用顺序执行
```

**问题2: 报告不更新**
```
解决：
1. 检查后端日志是否有错误
2. 刷新报告列表页面
3. 检查reports.json文件权限
```

### 9.4 前端相关

**问题1: API请求跨域**
```
解决：确认backend已启用CORS
CORS(app)
```

**问题2: 截图不显示**
```
解决：
1. 确认截图路径正确
2. 检查后端静态文件路由配置
3. 确认screenshots目录权限
```

---

## 十、最佳实践

### 10.1 用例设计

1. **命名规范**
   - 用例名称：`[模块]-[功能]-[场景]`
   - 步骤描述：简洁明了，体现操作目的

2. **元素管理**
   - 优先使用元素ID而非直接写xpath
   - 元素命名体现功能，如"登录按钮"而非"button1"

3. **变量使用**
   - 环境相关配置用变量（URL、账号等）
   - 动态数据用数据库变量
   - 敏感token用auth类型变量

### 10.2 测试执行

1. **截图策略**
   - 调试阶段：开启截图
   - 正式执行：关闭截图（节省空间）
   - 失败步骤：始终截图

2. **并行执行**
   - 用例间无依赖：可并行
   - 共享数据的用例：顺序执行
   - 注意资源限制

3. **错误处理**
   - 关键步骤添加元素存在检查
   - 合理设置等待时间
   - 记录详细错误信息

### 10.3 数据管理

1. **定期清理**
   - 旧报告及截图
   - 无用的变量配置
   - 过期的任务

2. **备份策略**
   - 定期备份JSON数据文件
   - 重要用例导出保存

3. **权限控制**
   - 生产环境数据库只读权限
   - 敏感变量加密存储

---

## 十一、技术亮点总结

1. **前后端分离架构**
   - Flask提供RESTful API
   - Vue3实现现代化SPA
   - 清晰的职责划分

2. **灵活的变量系统**
   - 支持多种变量类型
   - 按项目/环境隔离
   - 动态解析和注入

3. **智能元素管理**
   - 自动创建缺失元素
   - 元素库统一管理
   - 减少维护成本

4. **强大的执行引擎**
   - 基于Playwright
   - 支持并行/串行执行
   - 实时状态反馈

5. **完善的报告系统**
   - 详细步骤记录
   - 截图辅助分析
   - 多维度统计

6. **可扩展性设计**
   - 易于添加新操作类型
   - 支持多种数据库
   - 插件化的变量类型

---

## 十二、未来优化方向

1. **持久化存储**
   - 从JSON文件迁移到关系型数据库
   - 提升查询性能和并发能力

2. **用户权限管理**
   - 多用户支持
   - 角色权限控制
   - 操作审计日志

3. **定时任务调度**
   - 集成Celery或APScheduler
   - 支持Cron表达式
   - 任务结果通知

4. **用例录制功能**
   - 浏览器插件录制操作
   - 自动生成用例步骤
   - 导出到平台

5. **测试数据管理**
   - 测试数据构造器
   - 数据工厂模式
   - 数据隔离和清理

6. **CI/CD集成**
   - 提供CLI工具
   - Jenkins/GitLab CI集成
   - 自动触发测试

7. **性能优化**
   - 浏览器实例池
   - 分布式执行
   - 结果缓存

8. **监控告警**
   - 执行成功率监控
   - 失败用例自动重试
   - 邮件/钉钉通知

---

## 附录

### A. API接口速查表

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 用例 | GET | /api/data | 获取用例列表 |
| 用例 | POST | /api/add_case | 添加用例 |
| 用例 | GET | /api/testcase/<id> | 获取用例详情 |
| 用例 | PUT | /api/testcase/<id> | 更新用例 |
| 用例 | DELETE | /api/testcase/<id> | 删除用例 |
| 执行 | POST | /api/execute | 执行单个用例 |
| 执行 | POST | /api/batch_execute | 批量执行 |
| 报告 | GET | /api/reports | 获取报告列表 |
| 报告 | GET | /api/reports/<id> | 获取报告详情 |
| 报告 | DELETE | /api/reports/<id> | 删除报告 |
| 元素 | GET | /api/elements | 获取元素列表 |
| 元素 | POST | /api/elements | 添加元素 |
| 元素 | PUT | /api/elements/<id> | 更新元素 |
| 元素 | DELETE | /api/elements/<id> | 删除元素 |
| 任务 | GET | /api/tasks | 获取任务列表 |
| 任务 | POST | /api/tasks | 创建任务 |
| 任务 | POST | /api/tasks/<id>/execute | 执行任务 |
| 变量 | GET | /api/variables/list | 获取变量列表 |
| 变量 | POST | /api/variables/add | 添加变量 |
| 变量 | DELETE | /api/variables/<name> | 删除变量 |
| 项目 | GET/POST/PUT/DELETE | /api/projects | 项目CRUD |
| 环境 | GET/POST/PUT/DELETE | /api/environments | 环境CRUD |
| 数据库 | GET/POST/PUT/DELETE | /api/databases | 数据库配置CRUD |
| 数据库 | POST | /api/databases/test | 测试连接 |
| 系统 | GET | /api/settings | 获取设置 |
| 系统 | POST | /api/settings | 更新设置 |
| 日志 | GET | /api/logs | 获取日志 |
| 日志 | POST | /api/logs/clear | 清空日志 |
| 仪表盘 | GET | /api/dashboard | 获取统计数据 |

### B. 文件说明

| 文件 | 说明 |
|------|------|
| backend/app.py | Flask主应用 |
| backend/test_executor.py | 测试执行引擎 |
| backend/data.json | 测试用例数据 |
| backend/elements.json | 元素库数据 |
| backend/reports.json | 测试报告数据 |
| backend/tasks.json | 任务配置数据 |
| backend/variables.json | 运行时变量 |
| backend/variables_config.json | 变量配置 |
| backend/projects.json | 项目数据 |
| backend/environments.json | 环境数据 |
| backend/databases.json | 数据库配置 |
| backend/settings.json | 系统配置 |
| backend/app.log | 应用日志 |
| frontend/src/App.vue | 根组件 |
| frontend/src/router/index.ts | 路由配置 |
| frontend/src/components/*.vue | 业务组件 |

---

## 十三、改动历史

### 2026-09-18 14:30 - 项目认证方式配置改造

**改动内容：**
1. **项目数据模型扩展** - 新增 authType, loginUrl, username, password 字段
2. **前端 ProjectManage.vue** - 添加认证方式选择器和Cookie配置表单
3. **后端 app.py** - 修改项目CRUD接口支持认证配置

**技术要点：**
- 支持两种认证模式：Authorization（Token）和 Cookie（Web登录）
- 每个项目可独立配置认证方式，适配不同业务场景
- Cookie模式支持动态登录配置（登录URL、用户名、密码）

**文件改动：**
- [frontend/src/components/ProjectManage.vue](frontend/src/components/ProjectManage.vue) - 新增认证类型选择和表单
- [backend/app.py:2251](backend/app.py#L2251) - add_project 函数新增认证字段
- [backend/app.py:2279](backend/app.py#L2279) - update_project 函数支持更新认证配置
- [backend/projects.json](backend/projects.json) - 数据结构扩展

**待完成：**
- [ ] 实现 Cookie 登录接口 `POST /api/projects/<name>/login-cookie`
- [ ] 在测试执行时根据 authType 加载对应认证方式
- [ ] 清理旧的硬编码登录接口（login-for-cookie, login-for-admin-cookie）

---

### 2026-09-18 16:00 - 环境变量多环境同步与执行环境选择

**改动内容：**
1. **环境变量多环境同步** - 新增变量可同步到多个环境
2. **执行环境选择** - 用例执行时可指定使用的环境
3. **变量数据模型扩展** - environment字段支持逗号分隔的多环境

**功能详情：**

**1. 环境变量多环境同步：**
- 新建变量时可选择"是否同步到其他环境"（默认为"是"）
- 选择"是"后显示当前项目的所有环境列表，支持多选（默认全选）
- 变量的environment字段存储为逗号分隔的环境列表（如："测试环境,预发环境,生产环境"）
- 变量列表新增"环境"列，多环境以标签形式展示

**2. 执行环境选择：**
- 用例列表的设置弹窗新增"执行环境"选项
- 可选值为所有项目下的环境汇总
- 选择后，执行单个或批量用例时使用指定环境
- 覆盖用例自带的environment字段，影响变量解析和其他环境相关逻辑

**技术要点：**
- 前端使用NCheckboxGroup组件实现多环境选择
- 后端执行时判断config.executeEnvironment，如有则覆盖用例的environment
- 变量查询时支持解析逗号分隔的environment字段

**文件改动：**
- [frontend/src/components/EnvironmentManage.vue](frontend/src/components/EnvironmentManage.vue) - 新增同步选项和多环境选择
- [frontend/src/components/DataTableComponent.vue](frontend/src/components/DataTableComponent.vue) - 设置弹窗新增环境选择
- [backend/app.py:258](backend/app.py#L258) - settings接口支持executeEnvironment字段
- [backend/app.py:532](backend/app.py#L532) - execute接口支持环境覆盖
- [backend/app.py:663](backend/app.py#L663) - batch_execute接口支持环境覆盖

**数据结构变更：**
```json
// variables_config.json - 环境字段支持多环境
{
  "name": "api_url",
  "environment": "测试环境,预发环境,生产环境",  // 逗号分隔
  "type": "fixed",
  "value": "https://api.example.com"
}

// settings.json - 新增执行环境配置
{
  "defaultBrowser": "chrome",
  "defaultHeadless": false,
  "saveScreenshots": false,
  "enableMultiThread": false,
  "executeEnvironment": "测试环境"  // 新增
}
```

---

**文档版本：** v1.2  
**最后更新：** 2026-09-18  
**维护者：** 项目团队